% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration.R
\name{calibrate_all_lambdas}
\alias{calibrate_all_lambdas}
\title{Calibrate lambda for all borrowing methods}
\usage{
calibrate_all_lambdas(
  lambda_grid,
  scenario_base,
  drift_set,
  drift_set_cal = NULL,
  drift_set_confirm = NULL,
  nsim_cal = 500,
  nsim_confirm = nsim_cal,
  alpha = 0.025,
  alpha_cal = alpha,
  seed = 1,
  parallel = FALSE,
  ncores = NULL,
  robust = FALSE,
  eps = SMOOTH_EPS,
  gamma_li = 1,
  gate_c = 1.64,
  gate_tau = 0.25,
  gamma_mcp = 3,
  delta_bounds = DEFAULT_DELTA_BOUNDS,
  n_grid_opt = DEFAULT_N_GRID_OPT,
  two_stage = TRUE,
  n_fine = 6,
  primary_inference = c("sandwich", "model_based"),
  confirm_full_drift = TRUE,
  early_stop_drift = TRUE,
  stop_rule = c("point", "upper95"),
  select_rule = c("point", "upper95"),
  rho_mcp = DEFAULT_RHO_MCP
)
}
\arguments{
\item{lambda_grid}{Initial (coarse) lambda grid used for all
methods.}

\item{scenario_base}{A scenario list.}

\item{drift_set}{Default calibration drift grid when \code{drift_set_cal}
is omitted.}

\item{drift_set_cal}{Reduced drift set for the calibration stages.}

\item{drift_set_confirm}{Drift set for the optional confirmation
stage; \code{NULL} uses the calibration grid.}

\item{nsim_cal, nsim_confirm}{Replicates per drift value in
calibration and confirmation stages.}

\item{alpha, alpha_cal, seed, parallel, ncores, robust, eps, gamma_li}{As in
\code{\link{calibrate_lambda_grid_two_stage}}.}

\item{gate_c, gate_tau, gamma_mcp, delta_bounds, n_grid_opt, n_fine}{As in
\code{\link{calibrate_lambda_grid_two_stage}}.}

\item{primary_inference, confirm_full_drift, early_stop_drift}{As in
\code{\link{calibrate_lambda_grid_two_stage}}.}

\item{stop_rule, select_rule}{As in
\code{\link{calibrate_lambda_grid_two_stage}}.}

\item{two_stage}{Logical; use two-stage calibration if \code{TRUE},
otherwise single-stage.}

\item{rho_mcp}{MCP transition fraction in (0, 1), default 0.1.}
}
\value{
A list with \code{calibration_table} (long form),
  \code{calibration_summary} (per-method, per-lambda worst case),
  \code{lambda_star} (5 x 2 matrix indexed by method and
  inference), \code{lambda_star_table} (long form), and
  \code{method_calibrations} (per-method full output).
}
\description{
Runs lambda calibration for all five penalized borrowing methods
(Li adaptive lasso plus P1-P4) and returns a unified set of
calibrated lambdas under both model-based and sandwich inference.
}
